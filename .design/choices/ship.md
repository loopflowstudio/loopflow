---
choice: add_to_roadmap
reason: No roadmap exists yet - need to create one before scoping
options: [add_to_roadmap, scope_from_roadmap]
---

The `.docs/roadmap` directory doesn't exist and `.design/` is empty (no design docs).

The `scope_from_roadmap` flow requires an existing roadmap item to scope from, but there's nothing to scope. We need to first populate the roadmap with ideas before we can pick one to implement.

Choosing `add_to_roadmap` to bootstrap the roadmap with multiple perspectives (claude/gemini/codex models with different voices).
