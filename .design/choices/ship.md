---
choice: add_to_roadmap
reason: No roadmap exists yet - .docs/roadmap directory is missing
options: [add_to_roadmap, scope_from_roadmap]
---

The `.docs/roadmap` directory does not exist, and `.design/` contains no design documents. Since there's no existing roadmap to scope from, the appropriate next step is `add_to_roadmap` to establish what needs to be built before attempting to design and implement.
