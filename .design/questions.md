# Open Questions

## Infrastructure Roadmap

1. **Mypy vs Pyright** — The codebase has full type hints. Mypy is more common but pyright is faster and stricter. Which is preferred?

2. **Coverage threshold** — Once coverage reporting is live, what percentage should block PRs? 80%? Or start with visibility only and set thresholds later?

3. **PyPI package name** — Is the package published as `loopflow`? Need to confirm for trusted publishing setup.
