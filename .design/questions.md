# Open Questions

## Auto-rebase on merge conflict

User mentioned:
- When `lfpr land` fails due to merge conflict, automatically rebase using an LLM to resolve conflicts
- Add config option to disable auto-rebasing (e.g., `rebase: auto` / `rebase: manual`)
- Tell user when rebasing is happening

This is a follow-up feature, not part of the current PR.
