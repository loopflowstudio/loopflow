# refactor

New interactive prompt for collaborative re-architecture. Repo-local (`.claude/commands/`), not a shipped builtin.

## Review

**Verdict:** Ready to ship

No issues found. The prompt is well-structured with clear workflow, concrete examples, and good guardrails. The `requires`/`produces` frontmatter fields are documentation hints (not parsed config), which is fine.
