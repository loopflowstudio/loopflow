name: CI

on:
  push:
    branches: [main]
  pull_request:
  merge_group:

jobs:
  scratch-clear:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - name: Verify scratch/ is clear
        run: |
          if [ ! -d scratch ]; then
            echo "scratch/ is missing"
            exit 1
          fi

          EXTRA=$(find scratch -mindepth 1 ! -path 'scratch/.gitkeep' -print)
          if [ -n "$EXTRA" ]; then
            echo "scratch/ must be clear before landing"
            echo "$EXTRA"
            exit 1
          fi

  # Lint and test are split so the clippy compile and the test compile run on
  # separate runners in parallel instead of back-to-back on one critical path.
  # Distinct cache keys keep the clippy-artifact and rustc-artifact caches apart.
  rust-lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: clippy, rustfmt
      - uses: Swatinem/rust-cache@v2
        with:
          key: lint
      - name: Run cargo fmt check
        run: cargo fmt --all -- --check
      - name: Run cargo clippy
        run: cargo clippy --all-targets -- -D warnings

  rust-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
        with:
          key: test
      - name: Run Rust tests
        run: cargo test --all

  python-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: astral-sh/setup-uv@v7
      - name: Run Python tests
        run: uv run pytest python/tests/

  website-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: astral-sh/setup-uv@v7
      - name: Run website tests
        run: uv run python dev.py test
        working-directory: website

  e2e-smoke:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
      - uses: astral-sh/setup-uv@v7
      - name: Run E2E smoke test
        run: tests/e2e/test_smoke.sh
      - name: Run API smoke test
        run: uv run pytest tests/e2e/test_api_smoke.py tests/e2e/test_concurrent_clients.py -v

  docker-smoke:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
      - name: Verify Docker is available
        run: docker version
      - name: Run Docker executor smoke tests
        run: cargo test -p loopflow docker_ -- --nocapture

  swift-test:
    runs-on: macos-15
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0
      - uses: astral-sh/setup-uv@v7
      - name: Cache SwiftPM build
        uses: actions/cache@v4
        with:
          path: |
            swift/.build
            ~/Library/Caches/org.swift.swiftpm
          key: swiftpm-${{ runner.os }}-${{ hashFiles('swift/Package.resolved') }}
          restore-keys: |
            swiftpm-${{ runner.os }}-
      - name: Run Swift tests
        run: swift test --package-path swift
      - name: Check Swift multiplatform boundaries
        run: uv run python scripts/check_swift_multiplatform_boundaries.py

  concerto-ui-test:
    runs-on: macos-15
    steps:
      - uses: actions/checkout@v7
      - name: Install XcodeGen
        run: brew install xcodegen
      - name: Generate Xcode project
        run: xcodegen generate
        working-directory: swift
      - name: Cache Xcode DerivedData
        uses: actions/cache@v4
        with:
          path: swift/DerivedData
          key: concerto-dd-${{ runner.os }}-${{ hashFiles('swift/Package.resolved', 'swift/project.yml') }}
          restore-keys: |
            concerto-dd-${{ runner.os }}-
      - name: Run Concerto UI tests
        run: |
          python3 -u - <<'PY'
          import json
          import os
          import queue
          import re
          import subprocess
          import sys
          import threading
          import time

          cmd = [
              "xcodebuild",
              "test",
              "-project",
              "LoopflowSwift.xcodeproj",
              "-scheme",
              "Concerto",
              "-destination",
              "platform=macOS",
              "-derivedDataPath",
              "DerivedData",
              "-skip-testing:ConcertoUITests",
              "-resultBundlePath",
              "ConcertoTests.xcresult",
              "CODE_SIGNING_ALLOWED=NO",
              "CODE_SIGNING_REQUIRED=NO",
          ]
          timeout_seconds = 540
          result_bundle_path = "ConcertoTests.xcresult"
          success_summary_pattern = re.compile(
              r"^✔ Test run with \d+ tests in \d+ suites passed after "
          )
          xctest_all_passed_pattern = re.compile(r"Test Suite 'All tests' passed at ")
          swift_suite_passed_pattern = re.compile(r"^✔ Suite ")
          failure_pattern = re.compile(
              r"^✗ |Test Suite '.*' failed at |\*\* TEST FAILED \*\*|failed with \d+ failures"
          )
          # The Concerto GUI test host reliably fails to exit after the suite
          # finishes on headless macos-15, so xcodebuild hangs post-test. We
          # force-kill once success is proven (XCTest + >=30 Swift suites passed,
          # no failures). That verdict lands before this wait, so shrinking the
          # grace only trims dead idle time — it can't truncate a live run.
          idle_success_seconds = 15
          idle_min_swift_suites = 30

          def xcresult_indicates_success(path):
              if not os.path.exists(path):
                  return False, f"missing result bundle at {path}"

              try:
                  raw = subprocess.check_output(
                      [
                          "xcrun",
                          "xcresulttool",
                          "get",
                          "object",
                          "--legacy",
                          "--path",
                          path,
                          "--format",
                          "json",
                      ],
                      text=True,
                  )
              except subprocess.CalledProcessError as exc:
                  return False, f"xcresulttool failed with exit code {exc.returncode}"

              try:
                  payload = json.loads(raw)
              except json.JSONDecodeError as exc:
                  return False, f"xcresult JSON parse failed: {exc}"

              actions = payload.get("actions", {}).get("_values", [])
              if not actions:
                  return False, "xcresult had no actions"

              action_result = actions[0].get("actionResult", {})
              status = action_result.get("status", {}).get("_value")
              tests_count = action_result.get("metrics", {}).get("testsCount", {}).get("_value")

              if status != "succeeded":
                  return False, f"xcresult action status was {status!r}"

              if tests_count in (None, "0", 0):
                  return False, f"xcresult testsCount was {tests_count!r}"

              return True, f"xcresult status={status} testsCount={tests_count}"

          print("Running:", " ".join(cmd), flush=True)
          if os.path.exists(result_bundle_path):
              subprocess.run(["/bin/rm", "-rf", result_bundle_path], check=False)
          process = subprocess.Popen(
              cmd,
              stdout=subprocess.PIPE,
              stderr=subprocess.STDOUT,
              text=True,
              bufsize=1,
          )

          lines = queue.Queue()

          def pump_output() -> None:
              assert process.stdout is not None
              for line in process.stdout:
                  lines.put(line)
              lines.put(None)

          reader = threading.Thread(target=pump_output, daemon=True)
          reader.start()

          deadline = time.monotonic() + timeout_seconds
          saw_eof = False
          saw_success_summary = False
          saw_xctest_all_passed = False
          swift_suite_passes = 0
          saw_failure = False
          last_output_time = time.monotonic()

          while True:
              remaining = deadline - time.monotonic()
              if remaining <= 0:
                  # Kill xcodebuild first so it releases the result bundle lock,
                  # then check xcresulttool for the authoritative verdict.
                  subprocess.run(
                      ["/bin/ps", "-axo", "pid,ppid,etime,stat,command"],
                      check=False,
                  )
                  process.terminate()
                  try:
                      process.wait(timeout=15)
                  except subprocess.TimeoutExpired:
                      process.kill()
                      process.wait()
                  success, reason = xcresult_indicates_success(result_bundle_path)
                  if success:
                      print(
                          f"::warning::xcodebuild exceeded {timeout_seconds}s ({reason})",
                          flush=True,
                      )
                  elif saw_success_summary:
                      reason = "stdout showed final Swift test success summary before hang"
                      print(
                          f"::warning::xcodebuild exceeded {timeout_seconds}s ({reason})",
                          flush=True,
                      )
                  else:
                      print(
                          f"::error::xcodebuild exceeded {timeout_seconds}s ({reason})",
                          flush=True,
                      )
                  if success or saw_success_summary:
                      print(
                          "Timed out after tests completed; accepting successful test output.",
                          flush=True,
                      )
                      sys.exit(0)
                  sys.exit(124)

              try:
                  line = lines.get(timeout=min(1, remaining))
              except queue.Empty:
                  idle = time.monotonic() - last_output_time
                  if (
                      idle >= idle_success_seconds
                      and saw_xctest_all_passed
                      and swift_suite_passes >= idle_min_swift_suites
                      and not saw_failure
                  ):
                      print(
                          f"::warning::xcodebuild idle {idle:.0f}s after "
                          f"XCTest passed and {swift_suite_passes} Swift Testing "
                          f"suites passed with no failures; killing xcodebuild "
                          f"and accepting success",
                          flush=True,
                      )
                      process.terminate()
                      try:
                          process.wait(timeout=15)
                      except subprocess.TimeoutExpired:
                          process.kill()
                          process.wait()
                      sys.exit(0)
                  if process.poll() is not None and saw_eof:
                      break
                  continue

              if line is None:
                  saw_eof = True
                  if process.poll() is not None:
                      break
                  continue

              last_output_time = time.monotonic()
              sys.stdout.write(line)
              sys.stdout.flush()
              if success_summary_pattern.match(line):
                  saw_success_summary = True
              if xctest_all_passed_pattern.search(line):
                  saw_xctest_all_passed = True
              if swift_suite_passed_pattern.match(line):
                  swift_suite_passes += 1
              if failure_pattern.search(line):
                  saw_failure = True

          sys.exit(process.wait())
          PY
        timeout-minutes: 12
        working-directory: swift

  tests-result:
    if: always()
    runs-on: ubuntu-latest
    needs:
      - scratch-clear
      - rust-lint
      - rust-test
      - python-test
      - website-test
      - e2e-smoke
      - docker-smoke
      - swift-test
      - concerto-ui-test
    steps:
      - name: Aggregate test results
        env:
          SCRATCH_CLEAR: ${{ needs.scratch-clear.result }}
          RUST_LINT: ${{ needs.rust-lint.result }}
          RUST_TEST: ${{ needs.rust-test.result }}
          PYTHON_TEST: ${{ needs.python-test.result }}
          WEBSITE_TEST: ${{ needs.website-test.result }}
          E2E_SMOKE: ${{ needs.e2e-smoke.result }}
          DOCKER_SMOKE: ${{ needs.docker-smoke.result }}
          SWIFT_TEST: ${{ needs.swift-test.result }}
          CONCERTO_UI_TEST: ${{ needs.concerto-ui-test.result }}
        run: |
          failures=0

          check_result() {
            local name="$1"
            local result="$2"
            case "$result" in
              success)
                echo "PASS  $name"
                ;;
              skipped)
                echo "SKIP  $name"
                ;;
              *)
                echo "FAIL  $name ($result)"
                failures=1
                ;;
            esac
          }

          check_result "scratch-clear" "$SCRATCH_CLEAR"
          check_result "rust-lint" "$RUST_LINT"
          check_result "rust-test" "$RUST_TEST"
          check_result "python-test" "$PYTHON_TEST"
          check_result "website-test" "$WEBSITE_TEST"
          check_result "e2e-smoke" "$E2E_SMOKE"
          check_result "docker-smoke" "$DOCKER_SMOKE"
          check_result "swift-test" "$SWIFT_TEST"
          check_result "concerto-ui-test" "$CONCERTO_UI_TEST"

          exit "$failures"
