name: Packages (nightly)

on:
  schedule:
    # 09:00 UTC daily — before US work hours.
    - cron: "0 9 * * *"
  workflow_dispatch:

jobs:
  packages:
    uses: ./.github/workflows/package-build.yml
