pr land: fix landing from main branch

Landing from main now checks out main first before merge. Also closes PR and deletes remote branch after landing. Prevents "Must be on a feature branch" errors during PR create/update.