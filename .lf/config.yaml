agent_model: claude:opus
yolo: true
context:
  - docs
  - .design
exclude: "uv.lock"
push: true

pipelines:
  ship:
    tasks:
      - implement
      - rebase
      - polish
      - draft_commit
    pr: true
  submit:
    tasks:
      - polish
      - draft_commit
    pr: true
