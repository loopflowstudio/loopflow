agent_model: claude:opus 
yolo: true 
chrome: true

skill_sources:
  - name: superpowers
    prefix: sp
    path: ~/.superpowers
context:
  - docs
  - .design
exclude:
  - uv.lock
push: true

summary_tokens: 25000

summaries:
  - path: .
    model: claude:sonnet

branch_names:
  schema: "{user}.{name}.{ts}"
