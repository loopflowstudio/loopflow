# v0.10.1

## Changes since v0.10.0

- Target: default
- Tag prefix: (none)
- Area scope: (entire repository)

## Merged PRs

- #796 wave: reactive server replacing the goal loop — one persistent mind, chat/memory speech surface, harness engine (+23644 -3172, 152 files)
  - ## Try it  ```bash lf wave designer             # the wave's server: one persistent mind, until Ctrl-C lf chat "ship the button audit first"     # speak into its thread (any process can) lf memory add "buttons: variants unified" # curate what it knows lf wave s3                   # the s3 (control) VSM charter lf q worker run designer --flow build --task "unify button variants" bash scripts/demo_wave.sh    # the guided demo ```  ## Summary  Replaces the grind-loop `lf goal` command with a long-lived reactive wave server. `lf wave <name>` starts one persistent codex thread in the wave's own worktree that handles both progress and chat as a single conversation: messages queue mid-turn, steer redirects a live turn, interrupt finalizes a partial one. Truth is an append-only journal, so a restart replays the whole thread. `lf chat` and `lf memory` are the new speech surface — the same doors for minds, workers, humans, and scripts; a publish with no subscriber drops silently (exit 0) so the verbs are safe in any prompt. Worker reports arrive attributed in the thread. On the client side, Concerto swaps its tmux terminal-workspace surface for a live WaveChat pane.  ## Changes  - **Wave runtime** (`lfd/wave/`): new `journal`, `mind`, `registry`, `runtime`, `server`, `state`, and `memory` modules — the event-log spine, the one-thread mind, and the store-as-registry model (no daemon required). README documents the wire contract. - **Conversations harness** (`lfd/conversations/harness/`): unified engine over claude, codex, and opencode vendors with per-vendor mapping, conformance tests, and ndjson/jsonl testdata; a live-codex smoke test targets the 0.142.5 app-server protocol. - **lf commands**: delete `goal` and the standalone `loop`; add `chat`, `memory`, and `q`; new `session.rs` and `wave_context.rs` give every `lf run` ambient wave context. `chat`/`memory` join `KNOWN_COMMANDS`. - **Worker placement**: `--pool` (shared wave worktree, shared branch) and `--stack <run-id>` (fork a dependent series off an unlanded branch); default stays a fresh sibling worktree. Threaded through the Python CLI, client, and API with DTO fixtures and mutual-exclusion validation. - **Concerto**: WaveChat view, message rendering, and connection tests replace the removed `TerminalWorkspaceView`; `WaveOrigin` handles bundled-local vs remote lfd. - **Dispatch/transport**: one tmux wrapper (no env poisoning, probed liveness at every tier), reqwest transport, SIGHUP/SIGTERM cleanup. - **Store**: migrations rename `runs`/`wave_run_id` and add `run_id` to terminal sessions; new DTO fixtures and wire types shed defaults. - **Waves data**: rename `reduce→architecture`, `release→systems`, `desktop→concerto`; Asana is the roadmap, so file-based roadmap items are wiped and `GOAL.md`/`MEMORY.md` reconciled. - Extensive design and research docs land under `scratch/` (wave-agent design, prior-art briefs, free-energy synthesis).
- #801 wave realignment + the lfd/lfq/lfdb collapse (+13937 -28258, 286 files)
  - ## Usage  ```bash lf wave designer                          # start the wave's server: one persistent mind lf chat "ship the button audit first"     # post into its thread (any process can) lf memory add "buttons: variants unified" # curate what it knows tmux ls                                   # the wave agent and its workers are plain tmux ```  ```bash lf sub ...          # new subprocess/worker dispatch surface lf op queue ...     # queue reconcile / next, replacing the trigger machinery lf q ...            # query lfd (replaces the old Python `lfq` tool) ```  ## Summary  This collapses the daemon's sprawling subsystems into a single reactive wave model and drops the layers that no longer earn their place. The wave runtime moves out of `lfd/` to a top-level `wave/` module built around a persistent resident mind, an append-only journal, and a message channel — progress and chat are one conversation, human messages steer a live turn while attributed messages (workers, scripts) queue. The store layer is lifted out of `lfd/store` into its own `lfdb` crate module. The whole trigger apparatus (activation, ci_failure, cron ticker, loop_ticker, queue_reconcile, recovery, summary_refresh, watch) is deleted in favor of `lfd` verifying GitHub webhooks and translating them inward as attributed `lf chat` notifications — the mind decides how to react.  Large amounts of code retire alongside it: the Docker executor and its `bollard`/`tar` dependency tree, the entire Python `api`/`cli`/`client` surface (and the `lfq` tool it powered), the secrets subsystem across Rust/Swift/UI, and the wave scheduler. Observation is now plain tmux rather than a bespoke session API. Docs, demos, and DTO fixtures are rewritten to match.  ## Changes  - Wave runtime relocated to `rust/loopflow/src/wave/` — new `resident`, `runtime`, `server`, `supervisor`, `channel`, `wire` modules; one persistent codex thread per wave - Store moved from `lfd/store` to `lfdb`; migrations carried over, plus `050_drop_trigger_organs` - Trigger subsystem removed; GitHub webhooks (`hooks.rs`) rewritten to emit attributed chat notifications - Docker executor deleted (executor collapses to a single wave executor); `bollard`, `tar`, `schemars` and friends dropped from the lockfile - Python `api.py`/`cli.py`/`client.py` and the `lfq` tool removed; Rust `lf q` / `lf sub` / `lf op queue` take over dispatch and query - Secrets provider removed across Rust, Swift, and the Concerto Connections panel - Crons now live in `GOAL.md` frontmatter and fire as system turns; README, docs, and demos updated - CI drops the api-smoke and docker-smoke jobs; e2e/regression suites pruned to match
- #802 lf op: plan-first rebase and worktree placement (+1207 -72, 21 files)
  - ## Usage  ```bash lf op rebase --plan          # show the reset/rebase strategy without touching git lf op rebase                 # apply the planned update lf op rebase origin/main     # rebase onto an explicit target  lf op wt create my-feature --plan          # print the placement plan only lf op wt create child --stack parent       # create parent.child lf op wt create my-feature --main          # force a root branch from main lf op wt create feature --fork             # independent root from the review base ```  ## Summary  Both `lf op rebase` and `lf op wt create` now classify the branch and print a plan before mutating git. Rebase distinguishes disposable branches (reset to base), stack children (rebase onto parent), and authored work (normal rebase), stashing `scratch/` under `.lf/scratch-stash/` when a reset would otherwise blow it away and restoring it afterward. Worktree creation is modeled as an explicit placement request — root, main, stack, or fork — that resolves to a strategy (create root, create stack child, checkout existing, reuse worktree) you can inspect with `--plan`.  ## Changes  - New `PlacementRequest`/`PlacementPlan`/`PlacementStrategy` types in `worktrees.rs`; `plan_placement` and `create_from_placement_plan` split planning from execution. - `WorktreeSegment`/`StackBranch` newtypes enforce that dots are reserved for stack ancestry — use `--stack` for parent/child, hyphens within a segment. - `--plan`, `--main`, `--fork`, and optional-parent `--stack` flags on `lf op wt create`; `--plan` and explicit-target support on `lf op rebase`. - Collapsed `RebasePlan.scratch_stash` to a bool and dropped unwired enum variants. - Regression test for leading-space porcelain path parsing; e2e test `tests/e2e/test_rebase_efficiency.sh`; updated `docs/lfop.md`, `LOOPFLOW.md`, and goldens.
- #803 compress: drop postgres backend, lf q command, and docker lfd path (+1532 -5880, 120 files)
  - ## Usage  ```bash lf build "unify button variants" --wave designer --dispatch lf implement "Add the endpoint" --wave engbot --dispatch ```  Worker dispatch now goes through the flow commands (`lf build/implement/... --wave <name> --dispatch`) instead of the removed `lf q worker run`. Same wave semantics — workers inherit the wave's `GOAL.md` and `MEMORY.md` and report back via PRs.  ## Summary  A compression pass that removes three parallel code paths that no longer earn their keep. The Postgres `lfdb` backend is gone (SQLite is the single store), the `lf q` command surface is deleted in favor of flow-native dispatch, and the Docker/compose lfd service path is dropped in favor of direct per-OS service dispatch. Alongside the deletions, the conversation/harness plumbing moves out of `lfd` to the top level so it's no longer coupled to the daemon. Net ~4,300 lines removed.  ## Changes  - **Drop Postgres backend**: delete `lfdb/postgres.rs` and collapse `lfdb/mod.rs`, `catalog.rs`, `migrations.rs`, and `rows.rs` onto SQLite only. Removes the `deadpool-postgres`/`tokio-postgres`/`postgres-protocol`/`postgres-types` dependency tree (and its transitive `whoami`, `parking_lot`, `md-5`, etc.) from `Cargo.lock`. - **Remove `lf q`**: delete `lf/commands/q.rs` (~690 lines) and its wiring; workers now dispatch through the flow commands with `--wave`/`--dispatch`. README loses the `q worker run` examples and the `manual`/`loop` modes table. - **Drop docker lfd path**: delete `lfd/service/compose.rs` and fold service management into direct `cfg`-gated macOS/Linux dispatch; slim `scripts/concerto-dev.py` accordingly. - **Lift conversation plumbing out of `lfd`**: move `lfd/conversations/{turns,types}` into `chat/`, and the `harness/*` (claude/codex/opencode mappings, testdata, opencode runtime) to a top-level `harness` module. Move `wave_config` from `lfd/http/routes` into `engine`. - **Housekeeping**: shrink `lfd/config.rs`, add `wave/subscription.rs`, refresh docs (`lfd.md`, `waves.md`, `wave-authoring.md`, architecture) and goals `wave/goals` MEMORY, and update goldens/DTO fixtures to match.
- #804 release: v0.10.0 (+139 -20, 7 files)
  - ## Usage  ```bash git show v0.10.0:RELEASE_NOTES.md ```  ## Summary  Cuts the v0.10.0 release. Bumps the version to `0.10.0` across `Cargo.toml`/`Cargo.lock`, `pyproject.toml`, and `uv.lock`, and finalizes the release notes. This is the release where the **Wave** becomes loopflow's core primitive — a persistent, goal-driven looping agent that reads one `goal.md`, keeps its own `MEMORY.md`, works an Asana roadmap, and dispatches steerable sub-sessions. Around it the runtime collapsed to three nouns (Wave, Run, Session), and Postgres, `lfq`, the Docker `lfd` path, the conversations subsystem, Linear/Notion, and studio auth all left the tree. A net deletion, self-hosted by default.  ## Changes  - Version `0.9.12` → `0.10.0` in `Cargo.toml`, `Cargo.lock`, `pyproject.toml`, `uv.lock` - `RELEASE_NOTES.md` rewritten for v0.10.0 (Wave primitive, three-noun runtime, self-hosted-by-default, Concerto rename to Loopflow.app, release/CI machinery, breaking-change operational notes) - Archived the release under `release/v0.10.0/`: `DECISIONS.md` moved from `release/unreleased/`, and a new `NOTES.md` snapshot added
- #805 provider_auth: fall back to Doppler for OAuth client credentials (+205 -20, 2 files)
  - ## Usage  ```bash # Asana OAuth now works with just Doppler configured — no env exports needed doppler run -- lf op pm ... ```  OAuth client credentials (Asana and any PM provider) now resolve from the environment first, then fall back to fetching from Doppler when the env vars are unset. Previously, missing `ASANA_CLIENT_ID`/`ASANA_CLIENT_SECRET` (or the equivalent PM provider vars) failed hard with a `CommandUnavailable` error; now the broker shells out to `doppler secrets get <NAME> --plain` before giving up. The error message when both sources miss now mentions Doppler as an option.  ## Changes  - `oauth_client_credentials` is now async and threads a Doppler fetch through `read_oauth_client_credential`, which tries env then the secret runner. - New `fetch_doppler_secret` runs `doppler secrets get --plain`; the value is consumed inline and never printed. - Callers (`start_auth`, `complete_auth`, `refresh_pm_oauth_token`, `oauth_app`) updated to await the now-async credential lookup. - Extracted `read_nonempty_value` shared by env and Doppler paths. - Tests inject a fake Doppler runner via `oauth_client_credentials_with_doppler_runner` and cover env-preferred, Doppler-fallback, and both-missing cases, with an `EnvGuard`/`env_lock` to serialize env mutation.
- #806 wave: link concerto to asana, retire root/mobile/workflows waves (+3 -162, 7 files)
  - ## What changed  Wires the Concerto wave into Asana PM sync and retires three waves whose roles have been absorbed elsewhere.  ```diff  wave/concerto/GOAL.md  |  3 +++  wave/mobile/GOAL.md    | 16 ---  wave/root/*            | 122 ---  wave/workflows/GOAL.md | 24 --- ```  ## Summary  Concerto's `GOAL.md` gains a `pm` block pointing at its Asana project, so its loop can sync against the same PM backend the other active waves use. The `root`, `mobile`, and `workflows` waves are deleted — `root` was the conductor/garden wave, `mobile` was already archived and dormant, and `workflows` owned the engine machinery. Removing their `GOAL.md`/`README`/`backlog`/`yaml` files trims the wave map down to the waves that are actually looping, keeping scope legible and free of stale ownership claims.  ## Changes  - `wave/concerto/GOAL.md`: add `pm.provider: asana` + `asana_project` id - Delete `wave/root/` (GOAL.md, README.md, backlog.md, root.yaml) - Delete `wave/mobile/GOAL.md` (archived, no active surface) - Delete `wave/workflows/GOAL.md`
- #807 wave: retire the website wave wrapper (+0 -43, 2 files)
  - ## Summary  Removes the Website wave's steering files (`wave/website/GOAL.md` and `wave/website/MEMORY.md`), retiring the wave wrapper. This follows the earlier retirement of the root/mobile/workflows waves and the linking of concerto to asana — the website wave's orchestration harness is no longer needed to keep the public docs and site in lockstep with the library.  ## Changes  - Delete `wave/website/GOAL.md` — the ship-roadmap loop prompt and metrics for truing-up the public site - Delete `wave/website/MEMORY.md` — the wave's shipped/model/next memory (site import, one-docs-source, fly.io deploy)  Net: 43 lines removed, no product code touched.
- #812 pm: add Linear as a selectable PM provider (dormant) (+1528 -59, 6 files)
  - ## Usage  ```toml # .lf/config.toml [pm] provider = "linear"  [linear] team = "Loopflow" ```  ```markdown --- pm:   provider: linear   linear_project: "lin-123" --- ```  ```bash lf op pm sync   # now routes to Linear when the provider is set ```  ## Summary  Adds Linear alongside Asana as a project-management backend for `lf op pm`, plumbed through config, wave GOAL.md frontmatter, and OAuth. The provider is dormant by default — Asana remains the fallback — and only activates when `pm.provider = "linear"` (or a wave's `pm:` block) selects it. Linear authenticates via OAuth rather than a static API key, matching how the other providers resolve credentials.  ## Changes  - New `lfd/pm/linear.rs`: a GraphQL client for Linear covering team/project/issue create, list, update, complete, and comment, with rate-limit retries and paginated issue listing ranked by priority/sort order. - `PmConfig`/`LinearConfig` in engine config and a `provider`/`linear_project` block in `WavePmConfig`, so provider selection lives in both `.lf/config.toml` and GOAL.md frontmatter. - `ops/pm.rs` dispatches to the selected provider instead of assuming Asana. - `provider_auth` resolves Linear OAuth client credentials (with Doppler fallback), consistent with the Asana path.
- #813 pm: flip default provider to Linear (+128 -106, 27 files)
  - ## Usage  ```bash lf op auth linear                 # connect Linear with OAuth lf op pm init --wave designer     # connect/create the Linear project, write linear_project into GOAL.md lf op pm show --wave designer     # print the wave's live Linear roadmap ```  ## Summary  Flips the default PM provider from Asana to Linear. `.lf/config.yaml` now sets `pm.provider: linear` with a `linear.team` id, and every user-facing surface that named Asana as the roadmap backend now points at Linear — README, docs, builtin wave prompts, and test goldens. The roadmap still lives entirely in the PM tool with no local mirror; the change is which tool that is.  ## Changes  - `.lf/config.yaml`: default provider set to `linear`, adds `linear.team` - Docs and README: Asana → Linear throughout (`lf op auth linear`, `linear_project` frontmatter, roadmap-source-of-truth prose); notes Linear issue descriptions/comments are native Markdown - Builtin prompts (`LOOPFLOW.md`, build/design, govern/scan, ops/split-wave, ops/update-wave): roadmap-lives-in-Linear wording, `pm.linear_project` handle, Linear issue terminology - `ops/pm.rs` and wave `GOAL.md` files updated to reference Linear - Test goldens regenerated to match the new prompt text; builtins test now asserts on `Linear`
- #814 pm: remove Asana provider entirely (+103 -3671, 16 files)
  - ## Usage  ```bash lf auth connect linear lf op pm sync ```  Linear is now the sole PM backend. Asana is gone — its client, HTML/markdown conversion layer, OAuth code-paste flow, config, and wave `asana_project` field are all deleted. This lands the last step of the PM migration after Linear was made the default provider.  ## Changes  - Delete `lfd/pm/asana.rs` and `lfd/pm/asana_html.rs` (~2700 lines) plus the `AsanaClient` wiring in `pm/mod.rs`. - Drop `AsanaConfig` from engine config and `asana_project` from `WavePmConfig`; update parse tests to cover Linear instead. - Remove the Asana OAuth code-paste step (`complete_oauth_code_paste`, `extract_authorization_code`) from `lf auth connect` and strip Asana from `provider_auth`. - Generalize `lf op pm` output to say "roadmap project" / include the provider name rather than hardcoding "asana". - Delete `scripts/pm_reset.py`, `scripts/test_pm_priority_live.py`, and `test_pm_reset.py`; drop Asana from the `AuthProvider` Swift model and the architecture doc.
- #815 pm: land new Linear issues in Todo instead of Backlog (+113 -9, 2 files)
  - ## Usage  ```bash lf op pm update --create "Implement client" ```  New Linear issues now land in the team's active **Todo** column instead of Linear's hidden **Backlog**. `create_item` resolves the team's default `unstarted` workflow state (lowest `position`, e.g. Todo) and passes it as `stateId` on the `issueCreate` mutation. When a team has no unstarted state, `stateId` is omitted and Linear applies its own default.  ## Changes  - Added a `UnstartedWorkflowStates` GraphQL query and `unstarted_state_id` helper that picks the lowest-position unstarted state, returning `None` for graceful fallback. - Threaded `$stateId` through the `CreateIssue` mutation; `create_item` now resolves the state before creating the issue. - `WorkflowStateNode` gained a `position` field to rank candidate states. - Updated Linear and ops tests to expect the extra state-resolution request, plus a new test covering the no-unstarted-state fallback (null `stateId`).
- #817 wave docs: retarget PM references from asana to linear (+16 -17, 6 files)
  - ## Try it  ```bash git diff main -- wave/ ```  ## Summary  The PM provider flipped from Asana to Linear (Asana was removed entirely in `43a1f85c`). This branch scrubs the stale Asana references out of the wave documents so the authored roadmap, memory, and architecture notes match the live provider. The one functional change is `wave/concerto/concerto.yaml`, which now points at the Linear provider and project id instead of Asana; everything else is prose in `MEMORY.md`, analysis, and follow-up docs.  ## Changes  - `wave/concerto/concerto.yaml` — set `provider: linear` with the Linear project uuid, replacing the Asana provider and numeric project id. - `wave/goals/*` and `wave/systems/MEMORY.md` — rewrite "Asana is the roadmap" language to Linear; drop the resolved "Asana token expired" / "once auth restored" blockers now that the reconcile work targets Linear. - `wave/architecture/analysis/architecture-map.md` — update PM-mirror and PM-truth rows to name Linear. - `wave/goals/wave-agent-follow-ups.md` — point the `roadmap_item` and filing notes at Linear.
- #819 lf op release: make scheduled notes work without agent CLIs (+91 -10, 5 files)
  - ## Try it  ```bash cargo test -p loopflow release_notes --test release_tests ```  The new test stubs `lf --batch release-notes` to fail with a missing Claude CLI error. `lf op release notes` detects the missing-agent failure, falls back to deterministic notes, folds in `release/unreleased/DECISIONS.md`, writes `RELEASE_NOTES.md`, archives `release/v<version>/NOTES.md`, and clears `release/unreleased/`.  ## Summary  Weekly scheduled patch releases shouldn't depend on a human-installed Claude, Codex, or OpenCode CLI on the GitHub runner. `run_release_notes_step` still tries the agent-backed `release-notes` step first; when that step fails specifically because an agent binary can't be spawned, it now falls back to the existing mechanical note generator instead of failing the release. The fallback reuses the same release context — version, previous tag, target, merged PRs, and archived decisions — so the archive contract and intent source stay identical to the interactive path.  ## Changes  - `run_release_notes_step` catches missing-agent-CLI errors and generates deterministic notes rather than failing. - `is_missing_release_notes_agent_cli` narrowly matches errors that name an agent CLI *and* a missing-binary/spawn condition; all other `release-notes` failures still fail the release. - `generate_release_notes` now takes optional release decisions and emits a `## Release decisions` section, keeping the CI path's intent source aligned with the agent path. - Docs in `docs/lfop.md` and `release/README.md` note that scheduled release automation no longer requires a runner-local agent CLI. - New `release_notes_falls_back_when_agent_cli_is_missing` integration test covers the full fallback: mechanical notes, decisions carried through, PR titles included, `NOTES.md` archived, `release/unreleased/` removed.  Not included: this does not install an agent CLI in CI or add model credentials to the weekly workflow.  ## Validation  ```bash cargo fmt --check cargo clippy -p loopflow -- -D warnings cargo test -p loopflow --test release_tests uv run python scripts/test.py --all ```  `scripts/test.py --all` passed Python, Rust, website, Swift, and e2e. The local Concerto UI runner failed before bootstrapping with `Early unexpected exit ... signal kill before establishing connection`; no test assertion failed.
- #820 git: supply a committer identity when the environment has none (+71 -1, 1 files)
  - ## Try it  ```bash # On a headless host with no user.name/user.email (CI runner, cron Mac): cargo test -p loopflow commit_supplies_fallback_identity_when_none_configured ```  ## Summary  Headless hosts — GitHub Actions runners and the self-hosted cron Mac — have no `user.name`/`user.email` configured, so `git commit` refuses with "empty ident name" and automated commits (e.g. the release version bump) fail. This teaches `commit()` to detect a missing identity and supply a `loopflow` fallback committer via `-c user.name`/`-c user.email`. A developer's configured identity is never overridden — only a truly absent one (empty output or non-zero exit) triggers the fallback.  ## Changes  - `has_git_identity()` checks for both `user.name` and `user.email`; empty or failing lookups count as absent. - `commit()` injects `loopflow <loopflow@users.noreply.github.com>` only when no identity is configured. - Test isolates from global/system git config (`GIT_CONFIG_GLOBAL`/`GIT_CONFIG_SYSTEM=/dev/null`) to exercise the no-identity path deterministically and asserts the fallback author lands on the commit.
- #821 lfdb: sweep three pieces of confirmed-dead code (+28 -125, 7 files)
  - ## What changed  Removes three confirmed-dead pieces of code and adds a migration to drop the tables behind them.  - **`secrets_provider_config` table** (from migration 031) had zero references anywhere in `src/` — nothing ever read or wrote it. - **`wave_pr_merge_events` table** (from 009, recreated in 011) was insert-only via `record_merge_event`, with no `SELECT` anywhere; the writer and its only callers lived in test code. The writer, the `record_merge_event` methods on `Store`/`SqliteStore`/`WaveStateStore`, and the `QueueMergeEvent` DTO go with it. - **`worker_dispatched`/`worker_finished` serde aliases** on the journal's `RunObserved`/`RunCompleted` event kinds — the pre-rename compatibility shim is no longer needed, so the aliases and the replay-equivalence test are dropped. The test is replaced by a leaner one asserting new appends serialize under the post-rename names.  ## Migration  ```sql -- 051_drop_dead_tables.sql DROP TABLE IF EXISTS secrets_provider_config; DROP TABLE IF EXISTS wave_pr_merge_events; ```  `IF EXISTS` keeps the drops convergent across every db history.

_Generated mechanically for v0.10.1._