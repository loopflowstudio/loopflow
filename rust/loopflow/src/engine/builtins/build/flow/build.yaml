- kickoff
- review-design
- loop:
    skills:
      - code
      - xor:
          paths:
            demo:
              skill: demo
              description: "Observable change — walk through the experience"
            code-review:
              skill: code-review
              description: "Internal change — walk through the code"
    exit:
      router: gate
      paths:
        again:
          description: "Needs more work — iterate"
        done:
          description: "Ready to ship"
- deploy
