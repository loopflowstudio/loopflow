Controls suggest their usage. The interface teaches itself.

Norman: affordances and signifiers. The thing looks like what it does.

- Buttons look clickable, fields look editable, drag handles look grabbable
- Language matches user concepts; layout reflects relationships
- Interactive elements are distinguishable from static content
- Works for all input modes: keyboard, screen reader, pointer, touch
