# One bounded Wave iteration. The resident operates the Wave in a single turn —
# read, decide, and launch the one or two useful moves — rather than a
# clarify/pursue/mutate split. Its scheduler, not a loop-bit file, decides when
# another iteration is useful.
- wave/operate
