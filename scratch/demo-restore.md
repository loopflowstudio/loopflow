# Configured demo restoration

Finish: one supported development artifact set and Home, real current Wave
plans with the intended Tasks, one canonical repository root, and the simpler
outline using Loopflow's existing visual language. Check these before presenting
another window. A successful launch, fixture population or hidden warning does
not establish readiness. Human acceptance remains pending.

## Observed failures and bounded corrections

The human's screenshot establishes that the prior disposable app was visible.
It showed three missing-chapter warnings, duplicate loopflow roots, generic
unmatched Sessions and an empty content area. Earlier AX/activation observations
did not prove that the window was absent.

The selected installed development Home rejected the current migration frontier.
Its chapter table was empty; production had not applied the chapter draft.
Preserve both stores. Use supported fresh local promotion, then preview real
chapter transitions before any provider change.

Fresh local preflight initially rejected 32 placed Waves because its query
hardcoded the deleted `wave` wrapper. Runtime already invokes `wave/operate`.
Corrected that reference, and extended the isolated-copy test with a real placed
Wave. The focused test passes. Fresh preflight now resolves all 32 references
and reports `promote_and_migrate`. No persisted flow rewriting was needed.

Navigator combined canonical discovery with raw roadmap/selection paths before
deduplicating. Canonicalizing all sources produces one root. The new view test
keeps the exact Session reachable under that root. The existing deep-wine and
burgundy colors, cream text, shared serif/sans typography and operational Wave
lens now style the retained outline; hierarchy and pane ownership stay intact.

23 focused Swift tests pass (outline and mounted native Monitor/pane proofs).
All-target clippy and final formatting pass. The signed release app, CLI and
daemon were staged and promoted with `lf install promote --from-build ...
--fresh`, after its full artifact preview passed. The selected development Home
is `/Users/jack/.lf-dev/installed/local-f0d44aae116f45b2bbfb3658b06c6bf4`.
Doctor reports no migration error and matching install/store selection. Its
binary-freshness warning reflects the recorded dirty source revision, not a
store mismatch. Old Homes and production data remain preserved.

The private native render uses fixture planning and a hidden window. It confirms
burgundy selection/search, deep-wine rail, serif Wave names, operational lens and
readable light content. The initial probe omitted the app's theme environment;
correcting the probe to match the production theme restored content contrast.
Its temporary test is removed; source and final image are retained as evidence,
not another fixture-only demo or a configured-data verdict.

Actual installed roadmap, Session and active-Run reads succeed. The three current
Waves still have no chapter; this fresh Home has zero Sessions and live Runs.
Existing clients remain in their original Homes. Do not call that a positive
Monitor or conversation demonstration. Authoritative chapter previews for all
three Waves fail at Linear's token refresh (HTTP 400); a Doppler-supplied Product
retry also fails. `lf auth status linear` confirms expired. The supported
`doppler run -- lf auth linear` flow has opened the browser and awaits the human.
No chapter/provider planning writes have occurred.

[Source, binary and evidence hashes](demo-restore-evidence/receipt.json).

## Restoration and remaining human demo

### Authenticated counterexample: preserve the existing chapter Home

The human completed Linear authorization. Fresh authoritative previews returned
existing `demo-20260924` provider Projects for Product, Infrastructure and
Intelligence. A new rotation would move four started Tasks and abandon LOO-295
and LOO-296. Stopped that path; the missing chapter was local Home selection,
not missing provider planning.

Read-only inspection found the six complete chapter receipts in retained
installation `local-afee63d734c7482cb94d1071af26d9ea`, including these exact three
provider IDs. This is also the Home holding original Run history. The fresh Home
selected earlier lacks that history; treating it as the demo's correct data
source was a mistake. Both Homes remain intact. Candidate preflight against the
retained Home reports exact schema and 32 compatible executable references.

The installer lacked a route to select an older retained development Home.
Added `install promote --reuse-home <installation-id>` to its existing preview,
artifact and switch transaction. Selection comes from a settled development
receipt; no new registry, direct database repair or provider rotation is used.
The focused receipt test verifies that a later install does not lose the old
Home's identity/data, and lookup does not change the active selection. Actual
reselection and configured readback pass as recorded below.

Promoted the matching signed app/CLI/daemon with `--reuse-home
local-afee63d734c7482cb94d1071af26d9ea` after preview reported exact schema,
32 compatible references and no migration. Doctor now resolves that Home without
a migration error. All six chapter identities and receipt hashes match the saved
before state. Product exposes LOO-291/293; Infrastructure exposes LOO-285/292/295/296;
Intelligence has its valid empty chapter. No Tasks moved or expired. Linear auth
in this retained Home is also valid.

The installed app's bundled CLI supplies the real Swift reader and hidden native
render in `configured-capture.swift`. It passes with all three chapter IDs,
successful Session decoding and exact LOO-291 selection. The private image shows
one repository root, populated Tasks, restored styling and no missing-chapter
warnings. Its temporary test was removed after capture. This proves configured
reading/rendering, not human input or acceptance.

Five existing Sessions and three active Runs remain readable. The Sessions are
unbound in shared data; the Monitor must not invent Task attribution. One live
Run has exact Task ownership; two are unbound. Discovery is ready with no gaps.
LOO-291 retains its prior Task-worker credential-block evidence. Restoration does
not clear that history or restart a worker merely to make the view look healthy.

Final all-target clippy, formatting and the focused retained-receipt test pass.
The signed release build and real preview/apply/readback cover the recovery path.
Human composition/input acceptance and the broader Task proofs remain open.

No publication, Task completion or provider transfer occurred. Presentation is
the next step; launch alone will not establish acceptance.
