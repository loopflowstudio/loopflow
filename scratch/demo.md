# Human demo — 2026-09-23

## Human feedback — overall usability, 2026-09-24

Opened the current signed iteration 16 Loopflow Proof build for a manual demo.
All eleven source/binary hashes in its receipt matched at launch (PID 44427).
Asked the human to try a fresh LOO-291 conversation, draft retention through
navigation, and a companion terminal. The human responded:

> The app is still jsut confusing me overall

The demonstration is unsuccessful at the overall usability level. No draft,
terminal-retention, or interaction approval follows from this response. The
specific confusing controls, intended first action, and expected screen remain
unknown; do not invent a diagnosis from the earlier technical receipts. Pause
the scripted retention trial and clarify the human's intended use before choosing
a UI correction. No implementation change or Task completion is claimed.

## Earlier demo history

In progress; no human confirmation yet.

The human changed direction during the demo: Active Tasks first, with internal
queries behind named views and no query syntax in the UI. The disposable demo is
`/tmp/loo291-active/Loopflow Active.app`, using the matching local CLI and live
Home `/Users/jack/.lf`. The launch at PID 50304 produced the user's 21:42 screenshot:
LOO-285, LOO-279, LOO-226, LOO-293 and LOO-291. This supersedes the empty development-
Home launch at PID 38209. Receipts live under `/tmp/loo291-active/`.

The user reports LOO-279 flickering. Worktree membership is being added so its
row survives provider gaps. This is a reported failure, not a completed demo.
No installed binary was replaced and no user provider was transferred or resolved.

Opened the signed `/tmp/loo291-iteration8/Loopflow Proof.app` with the actual
Task checkout and the same explicitly selected installed development Home as
the configured provider receipts. Production Swift files have no delta since
the checkpoint used by that proof; no installed app replacement or provider
launch was performed. Launch receipt: `/tmp/loo291-human-demo/launch.log`.

Demonstrate readable light/dark appearance, actual Task directive and scoped
New conversation, then Session-row return into that same native shell with
draft and companion retention across worktree groups/splits. Let the human
operate the UI; this runner's Accessibility permission is not needed for manual
interaction. Launch success alone is not demonstration success. Keep existing
user Sessions untouched unless explicitly selected by the human.

## Current relaunch

Owned previous demo PID 50304 accepted termination; the updated signed bundle
opened as PID 44263. Receipt: `/tmp/loo291-active/worktree-launch.log`. App hash
`5b13402fa6b71ac94192e239df6ce4ecac3645d56d1753f0ad74c8748c550dd6`; helper hash
`812b3b7183c61c84c1f5ec55fdef308b5b85562ee5d9e81615d7f2dbef1b836c`.
Both builds passed. Actual roadmap workspace checks match filesystem existence
for every referenced Task in this repository; seven exist, including completed
LOO-279 and LOO-278. The live provider criterion may retain additional rows.
Receipt: `/tmp/loo291-active/worktree-roadmap.json`.

Requested human observation of LOO-279 stability and current working-set fit.
Confirmation remains pending; no configured visual pass or full Task completion
is claimed. Shared-schema and query tests pass as recorded in task-views.md.

## Current Session discovery relaunch

Human counterexample: this very conversation should appear under LOO-291.
The preceding demo read a different Home and the shared Session scanner omitted
interactively resumed headless Runs. Shared discovery/subject-resolution fixes
and exact live receipts are in session-discovery.md. Demo PID 41489 now uses the
conversation's owning Home. Current conversation remains alive in PID 37189;
no transfer occurred. Human confirmation remains pending. The earlier main-Home
working-set screenshot does not validate the current Session population.

## Human screenshot — 2026-09-24 07:21 PDT

The human confirmed Active is stable and matches the working set. Their next
screenshot shows readable light appearance and a terminal/count control of 2
on LOO-291. The menu is collapsed: this proves two Session associations are
presented, but not the displayed title/state of either individual conversation.
LOO-293 has provider activity without a Session control. Other open Sessions
include two generic scratch-artifact titles and a task-viewer review Session.
Do not treat the screenshot as confirmation of exact Session selection or transfer.
