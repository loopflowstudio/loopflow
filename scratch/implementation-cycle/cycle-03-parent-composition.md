# Parent composition correction

The implement pass's native capture retained a dark sidebar and an always-open
Activity column. Compared its `cycle-03-evidence/wave-false.png` with the accepted
`/tmp/loo291-accepted-frame-reference.png`: the latter has one cream sidebar and
a wide main surface. The implement report's claim that composition was as intended
does not establish fidelity to that accepted reference.

Corrected the existing native owners: the navigator consumes the shared palette,
uses its muted cream surface and tinted selection, and has a 272-point width.
Task labels/title use the shared sans face; removed the old minus glyph. Activity
is optional through the existing breadcrumb and a repository/window navigation
boolean, initially hidden. The existing Activity reader/control remains the
only implementation. Wave breadcrumbs now render too. Removed the internal
chapter-ID heading above Current KRs; moved machine/turn controls behind a
Wave-controls disclosure after planning content.

Extended the existing mounted retention proof to toggle actual Activity controls
and assert visibility while preserving its existing surface, draft, viewport,
focus and companion assertions. The capture now selects compact presentation
for the Wave rather than accidentally recording flat Sessions. This remains
fixture planning and owned native PTYs, not configured data or human acceptance.

The mounted proof passes both parameterized cases in
`/tmp/loo291-c3-parent-composition.log`; inspected Wave and Task captures under
`/tmp/loo291-c3-parent-captures`. They now show the cream sidebar and full-width
main surface. A final tint-only edit makes controls inherit the brand accent;
the next compression compile must include that edit. Compression and independent
review follow.
The implement receipt retains its original source attribution and is not silently
relabelled as a pass for these changes.
