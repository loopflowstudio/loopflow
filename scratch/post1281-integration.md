# Main after #1281: integration and pending demo

Rebased locally onto `236086c946dfef0c9c44c090aea7821da7102aa2` through
`lf rebase --manual main` and `lf rebase --continue`. The initial attempt
stopped before creating a sequencer with uncommitted contributions present;
checkpoint `cf65d5b5c` preserved those contributions before retry. Rebased head
is `a9342cec2`; reconciliation corrections are committed as `c64ef87f4`.
No push, PR publication, Task completion, installed-app replacement or live
chapter application occurred.

The earlier integrated chapter commits overlapped #1281. Kept main's chapter
ownership, metric targets, historical readings, transfer/retry machinery and
unavailable-plan behavior. Preserved the unified outline, active Run stream,
retained native panes and direct Work-bound flows. Main's chapter operations,
chapter tests, metrics, shared chapter types and Wave details match exactly.
The remaining shared roadmap delta is this branch's current-Wave filtering,
explicit repository scope and local checkout existence.

Resolved the CLI dispatcher using working-directory selection from main and
this branch's unified skill/flow dispatch. Updated an auto-merged caller to the
renamed live-process receipt helper. Adapted the transfer proof to chapter
source identity after main removed runtime Project IDs. The inspector now proves
Wave objective and chapter KRs rather than the removed chapter objective.
Kept both history-without-current-plan and delayed-history-navigation tests.
The flow proof now expects main's declared issue selector; exact durable Task
context and unchanged managed Flow remain asserted. Regenerated prompt goldens.

Verification: 36 Swift tests across five suites pass, including real Ghostty
PTY draft/companion retention through chapter transfer. Eight Rust DTO tests and
11 initial flow tests pass; the remaining bound-flow test initially failed its
obsolete selector expectation, then passed after correction. Prompt golden
comparison, cargo fmt and all-target clippy with warnings denied pass. Earlier
compile failures are retained. No broad gate or configured vendor trial is claimed.
[Receipt and source hashes](post1281-evidence/receipt.json).

## Demo is pending human observation and a usable configured Home

Signed disposable `/tmp/loo291-post1281/Loopflow Review.app` launched as PID
33970 using its copied rebased CLI and the selected installed development Home
`/Users/jack/.lf-dev/installed/local-198f5df536e545b7b63b3d15abeacdd2`.
The installed app and existing provider clients were untouched. LaunchServices
reported success, but activation remained false. The exact AX probe reports
untrusted and Windows result -25211; no global input or permission change ran.
Human has not confirmed a visible window or accepted the composition.

Read-only roadmap returned 50 Waves and reports no current chapter for Product,
Infrastructure and Intelligence. The same Home's doctor reports an incompatible
database. These are observations from an incompatible Home, not approval to
create/move/cancel planning work or delete/rebuild the store. A later installed
`lf task changes` read also fails with “candidate removed or canonicalized an
applied draft” and recommends fresh local promotion. No repair or promotion ran.
The worktree-local old development Home separately reports an older canonical
frontier. Do not relabel these as healthy empty planning or UI failures.

Next: restore a compatible configured development Home through supported
promotion, then confirm/apply the intended first chapter through its explicit
preview/acceptance boundary before claiming the full Task/Monitor demo. The
current app is only a launched review surface. Preserve the existing provider
clients and ask the human to confirm the visible app before further interaction.
