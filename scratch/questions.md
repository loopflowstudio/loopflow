# Open Questions

- Should `wave_rename_renames_branch` (intermittent in `cargo test --all`) be fixed in this PR scope or tracked as a separate follow-up?
- Should `ScreenshotPipelineTests.testCapture` be made deterministic now (foreground activation/wait strategy) or handled in a dedicated UI test stabilization pass?
