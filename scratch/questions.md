# Open Questions / Assumptions

- Assumption: "non-trivial diff" for PR message validation means at least 20 changed lines (`+`/`-`, excluding `+++`/`---` headers). This threshold can be tuned if we want stricter/looser body requirements.
