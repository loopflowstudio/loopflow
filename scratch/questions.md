# Gate assumptions

- Browser-facing website tests and generated Xcode project UI tests were skipped because this headless run was declared as having no rendering environment. Non-rendering CI checks were run instead.
