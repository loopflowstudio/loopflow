# Open questions / assumptions

- Assumed `ship` and `ship-roadmap` should use `ops: land --create-pr` (not bare `ops: land`) to preserve existing land semantics where PR creation is opt-in unless explicitly requested.
