# Goal Review Notes

No open implementation questions are being carried forward for the next
`lf code` run. Earlier notes that kept dispatch compatibility, taskless
`WaveRun` launch, raw dict adapters, or local `lf goal` rendering are
superseded and shipped.

The one carried-forward gap — Wave ancestry was dropped from the durable model,
so `WaveAgentTree.child_waves` is always empty — is now folded into
`wave/goals/2-wave-ancestry.md`. Pick it up there.
