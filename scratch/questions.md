# Open questions

None for this implementation. Placement-aware orchestration remains a future
Home-layer command; ordinary `lf` commands are machine-local. Public addresses
assigned through NAT cannot be discovered reliably from the guest, so durable
automatic startup should name the stable HomeId; an explicit SSH destination
is recognized for its foreground invocation.
