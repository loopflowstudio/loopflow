# Open Questions

- Portfolio dashboard wave selection still uses timed notification retries when opening a repo window. Should this move to an explicit window-ready handshake?
- Portfolio dashboard cards capture connection state at creation time. Should connection changes trigger card-state rebuild/rebind?
