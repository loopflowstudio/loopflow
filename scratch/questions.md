# Open questions

- Implemented repo onboarding from `scratch/lfd-repos-repo-onboarding.md` (path-based `POST/DELETE /v0/repos`). Area docs also describe a newer name-based `register/add/unregister` design. Confirm which API contract should be the long-term target before follow-up iterations.
