# Questions / assumptions

- Assumed the mobile wave should become strictly view-only now, so the active mobile chat item docs were removed from `wave/mobile/` and preserved only as backlog notes.
- Assumed governance surfaces (runboard, portfolio, calibration, beat programming, release controls) belong under `workflows` even though they render in Concerto, because the reorg says those items follow engine/governance ownership rather than desktop chrome ownership.
