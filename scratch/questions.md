# Open questions

- Assumption: the default multiplexer layout now swaps the old runs pane for `roadmapDetail` so the new list/detail roadmap pattern is visible immediately on fresh workspaces. Runs remain available from the pane palette and split cycling.
