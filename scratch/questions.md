# Open assumptions

- Implemented the richer `ContextSnapshot` contract from the area docs (`source_counts` + per-file `documents` drill-down) even though `scratch/02-context-ui.md` still describes the earlier metadata-only variant.
