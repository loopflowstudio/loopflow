# Open Questions / Assumptions

- Assumed file-based encryption-key fallback path `~/.lf/provider-token.key` is acceptable when macOS Keychain (`security`) or Linux Secret Service (`secret-tool`) is unavailable (Docker/headless/CI).
