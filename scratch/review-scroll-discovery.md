# Scroll refresh and discovery review — 2026-09-24

The scroll/refresh measurement advances the accepted canvas. A fresh public
command passes 24/24 native observations. The rejected environment-only discovery
candidate is absent from current source; all five restored ownership tests pass.
No additional executable correction was established. Complete canvas publication
remains pending.

| Claim | Planned behavior | Implemented behavior | Proof | Result |
|---|---|---|---|---|
| Scroll during shared refresh | Updated final Task remains visible with viewport, selection and Sessions retained | Native scrolling while the real Podium reader awaits fixture transport | Fresh short public runner, both populations | pass at capture/OCR scope |
| Retained workspace | Original terminal surfaces, draft and companion remain usable | Existing SessionsView, multiplexer and Ghostty owners | Same runner's retained input and PTY replies | pass with fixture reads and owned PTYs |
| Repeatable measurement | Fixed populations, all attempts retained, first/warm separate | Twelve scenarios over 8/256 Tasks | Fresh 24/24; prior 504/504 artifact and source hashes verified | pass for declared endpoint |
| Existing native ownership | Old waiting clients remain discoverable without new publication; dead clients disappear | Restored receipt traversal and PID/start validation | Fresh five-test Rust ownership pass | pass locally |
| Bounded discovery and automatic refresh | Preserve ownership while avoiding repeated retained-history discovery | Still enumerates Run directories; Monitor refresh remains explicit | Rust snapshot and Podium/RegistryQuery source | gap |
| Full acceptance | Complete rendering measurements, ownership integration, both builds and human/configured trials | Earlier scoped evidence remains; no new full acceptance | Governing directive and current source | gap |

## Fresh proof

```sh
uv run python scripts/desktop_performance.py run --output /tmp/loo291-review22-native --samples 1
cargo test -p loopflow --lib run_record::active::tests
```

Both exit zero. The native command records 24 observations, no journal errors,
no unstarted samples, and identical source fingerprints before/after. The final
Task changes from Task 007/255 to Updated 007/255 in captured text. Settled offsets
remain exactly 173 and 12522 points respectively. The subsequent workspace cases
verify original surfaces, actual first responder, retained draft and PTY replies.
This is discrete native scrolling through fixture transport, not wheel delivery,
continuous gesture smoothness, compositor timing, configured discovery or human
acceptance. No p95 or budget follows from this one-attempt short run.

The five Rust checks include the two cases that failed under environment-only
discovery: existing native receipts without a new capture binding, and old waiting
Runs with exact Task attribution in a shared checkout followed by client exit.
They also cover deduplication, invalid ownership and successive Runs in one Exec.
These tests use owned local processes; they do not demonstrate vendor providers.
Logs and source/artifact hashes are in [the receipt](review22-evidence/receipt.json).

## Source and next design boundary

Read the cached LOO-291 directive through `lf pm show --wave product --no-sync
--json`, current design, Product memory, prior reviews and candidate failure
receipts. The complete tracked patch is `/tmp/loo291-review22-tracked.patch`:
735 sections, 700 identical to the preceding complete review snapshot and 35
changed/new. Three prior scratch sections are absent. Reused prior source reviews
for unchanged content and inspected the changed measurement, reporting,
documentation and operating-guidance paths. The removed canvas document was read
from checkpoint 03105f382; the current directive preserves its governing scope.
Untracked candidate and measurement evidence was inspected separately.

Traced the held fixture response through RegistryQuery, Podium publication,
outline rendering and native viewport. Its test-only continuation adds no product
reader or state owner. The corrected probe compares the settled pre-refresh offset,
avoiding the recorded lazy-content-height counterexample. The benchmark still
does not diagnose rendering phases or establish an optimization benefit.

Negative searches retain one Podium caller per roadmap, Session and active-Run
query, one root workspace registry and one production capture-binding writer.
Removed SessionScope, FlowResolutionAction, requestedSessionId and
routing_project_id remain absent from the inspected production paths. The seven
rejected-candidate files match checkpoint 510a48b44. Monitor still labels its
observation time and offers explicit Refresh; no environment scanner or automatic
active-Run poll survives.

The proposed next design needs an explicit reader lifetime. RegistryQuery's
activeRuns invokes the one-shot CLI transport; a cache inside that subprocess
would cold-scan on every request. Resolve how one Rust reader survives observations
and is owned/canceled by the shared Mac reading boundary. Preserve selected Home
authority and existing receipts. Specify notification subscription versus initial
scan ordering, publication/removal races, lost-event recovery, and explicit
unavailable observations during rescan. Prove cold/warm costs with large retained
history plus older live clients before adding automatic refresh. This is design
direction, not approval or implementation of a new daemon or durable index.

Compositor/hitch measurements, correlated production phases, configured costs and
budgets, complete objective/target integration, fallback compilation and the human
composition demo remain open. Preserve positive configured Run-change proof and
the original external trials/authorized edit. No optimization Task, installation,
PM mutation, publication or Task completion occurred.

The [review-slice skill](/Users/jack/.agents/skills/review-slice/SKILL.md) permits
publication “When all applicable `Done when` claims hold and the slice is coherent.”
The remaining core and acceptance gaps do not meet that condition. Recorded the
discovery counterexample and reader-lifetime requirement in `.lf/directions/desktop.md`.
