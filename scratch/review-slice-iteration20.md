# Task Monitor review — 2026-09-24

The Monitor increment advances the agreed canvas. A fresh configured app trial
opens LOO-291's Monitor from its exact planning row, agrees with the CLI's empty
Task observation, accepts Refresh, and retains directive inspection and outline
presentation changes. No additional production defect was established in this
review. The preceding pane-identity and native-focus corrections remain intact; a further
concurrent pane-choice correction passes fresh focused proof.
The complete canvas is not ready to publish.

## Evidence matrix

| Claim | Planned behavior | Implemented behavior | Proof | Result |
|---|---|---|---|---|
| Task selection opens passive Monitor | Exact Task subject, no provider launch | LOO-291 opens its Monitor and observation controls | Fresh configured native app, exact planning UUID; Session IDs unchanged | pass for observed path |
| Task Runs agree with shared evidence | Typed Work join; complete emptiness differs from missingness | Configured Monitor and scoped CLI both show zero Runs with no gaps | Fresh sequential app/CLI comparison; not an atomic inventory | pass for empty path |
| Positive, stale and incomplete observations | Retained pane subject; errors cannot claim healthy emptiness | Exact Task filtering and last-good/gap presentations | Existing TaskMonitorTests receipt, source hashes unchanged | pass locally; configured positive case remains open |
| Sessions, Monitor and companion share one layout | Retained drafts, focus, split, resize, zoom and Close | Existing multiplexer plus observation responder; saved choice includes pane content | Existing six-test receipt plus fresh three-test pane-choice/real-PTY proof | pass at recorded local scope |
| Planning remains reachable | Inspect directive and retain selection through compression | Inspect opens the directive; Full presentation retains selected Task | Fresh configured AX actions; no directive edit | pass |
| One authority per responsibility | Shared Rust ownership; Podium read; retained window layouts/surfaces | One active query caller, capture writer, attribution resolver and root workspace registry | Source trace and negative searches | pass |
| Complete canvas and Task acceptance | Bounded discovery, both measured experiences, both builds and human/external trials | Demand refresh only; historical traversal remains; native build passes | Source, fresh native build, prior fallback preflight failure and accepted design | gap |

## Fresh configured proof

Built `LoopflowMac` from this checkout and signed a temporary review bundle.
Selected the existing installed development Home explicitly, with aligned control
and data paths and this checkout's CLI. The probe uses real planning and Session
reads, not fixture app mode. Its target is planning Task
`ee671927-255f-41e6-8429-b830d59cc1de` (LOO-291).

The [executed probe](monitor-evidence/review20/configured.swift) and
[passing log](monitor-evidence/review20/configured.log) establish:

- Exact Task-row selection exposes Monitor with Refresh and an observation time.
- The displayed confirmed-empty state agrees with `runs --active --task LOO-291
  --json`: zero Runs and zero gaps. This does not demonstrate positive configured
  Run appearance/disappearance or an atomic cross-surface inventory.
- Refresh accepts AXPress. The probe does not claim a separately verified newer
  observation or measured refresh latency.
- Inspect exposes the Task directive. The existing presentation menu offers
  Compact, Full hierarchy and Sessions; selecting Full retains the Task and
  inspection content.
- Session identities are unchanged across the trial. No Session open, transfer,
  resolution, provider input, PM write or worktree creation was requested. The
  owned temporary app is terminated afterward.

The first bundle trial accidentally retained the old executable under the
bundle's declared `Loopflow` name; copying the new binary to `LoopflowMac` did
not replace it. That attempt supplies no current Monitor verdict. The corrected
bundle exposed Monitor, but SwiftUI inherited its container identifier onto child
controls, so the probe's child-identifier lookup failed. The final probe locates
the visible Refresh button by role and label after finding the Task-bound Monitor.
One probe compile error also caused an obsolete probe binary to run; its separate
log is not a new product failure. These attempts remain recorded, not relabeled
as successful checks.

This is configured read-only navigation through accessibility, not a screenshot
approval, provider conversation, rendered latency measurement, or human acceptance
of the simplified composition. Native input/retention evidence remains the
existing mounted real-PTY receipt.

## Source review

Read the governing Task/design scope, canvas launch decision, current Monitor
implementation notes, preceding reviews and native test receipts. Captured the
unrestricted base-to-working tracked diff at `/tmp/loo291-review20-tracked.patch`:
455 sections, 444 identical to review 19 and 11 changed. Examined the changed
executable paths and the untracked Monitor view/tests separately; unchanged
historical code retains its earlier review rather than receiving a new broad gate.

Traced outline Task selection through `openTask`, `showMonitor`, saved PaneState,
the window registry, outer checkout layout, inner multiplexer and native responder.
Task placement uses its existing checkout when present and the repository otherwise.
Session leaves still restore their exact retained terminal. Monitor adds beside
occupied panes and has no terminal identity or lifecycle action. Saving expected
pane content prevents a reused pane ID from restoring another Task's Session.

Traced `TaskMonitorView` through Podium's single in-flight Home read and
RegistryQuery to the shared Rust snapshot. The pane joins planning identity to
runtime Work, independently of navigation selection or checkout. Errors preserve
last-good evidence and gaps prevent a confirmed-empty claim. The Rust path retains
native receipts from old launchers and exact capture intervals, deduplicates process
observations by Run, and does not use the historical age/count cap. Its retained
directory traversal is still unbounded. Demand refresh avoids adding frequent
polling before that obligation is settled; it does not settle it.

Negative searches retain one production `query.activeRuns` caller, one root
SessionsWorkspaceRegistry, one capture-binding writer and one shared Work resolver.
SessionScope, FlowResolutionAction and the rejected clearFocus path remain absent.
There is no Monitor-specific layout tree, surface pool, process controller or
per-pane poller. The new observation responder belongs to presentation only.

All nine Monitor and fourteen active-Run hashes matched before configured proof.
The final check caught a concurrent change in SessionsView and TaskMonitorProofTests:
`rememberTaskPane` now rejects a Session whose shared subject differs from the
selected Task. Without that check, clicking another Task's retained pane could
replace the selected Task's saved choice. This correction is the other writer's
contribution and is preserved. It adds no new owner or inferred cwd association.

Inspected the two-line correction and its mounted regression, then freshly ran
`swift test --package-path swift -Xswiftc -gnone --jobs 4 --no-parallel --filter
TaskMonitorProofTests`: all three tests pass, including the new pane-focus case,
reused-pane return and retained real-PTY input. [Log](monitor-evidence/review20/concurrent-pane-proof.log).
The other 21 hashes still match. The earlier six-test and Rust/Swift contract
passes remain prior receipts. The configured bundle predates this concurrent
correction; its exact binary hash is retained in the
[review receipt](monitor-evidence/review20/receipt.json), separately from final
source hashes. Fresh native build, configured probe and signature verification
apply to that bundle; the focused three-test pass applies to the corrected source.
This review authored no executable production change.

## Disposition

Continue the core with bounded live discovery that preserves existing clients,
shared automatic refresh, and the defined `hierarchy_interaction_ms` and
`task_workspace_ready_ms` runners. Collect honest rendered/usable baselines and
frame-hitch evidence before optimization; retain failures and unavailable hosts.
The fallback build still has no compiler verdict after its recorded resource
preflight failure. Fresh native build success does not replace it.

The simplified composition still requires the human demo, positive configured
activity changes and combined-pane experience. The original human-selected external
trials, authorized directive edit and long-lived-registry measurements/budgets also
remain. Do not move those into the deferred optimization Tasks or close LOO-293.

No publication, installation, Task completion or PM mutation occurred. The
[review-slice skill](/Users/jack/.agents/skills/review-slice/SKILL.md) requires
“all applicable `Done when` claims hold” before publication. Bounded discovery,
measurements, fallback compilation and acceptance remain unmet, so publication
stays pending.
