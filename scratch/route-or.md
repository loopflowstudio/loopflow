path: tune

Adjustments are needed this cycle. The chord is not in tune while live tend runtime is unavailable across all member waves, `signals` is stalled at design-doc stage despite being a Phase 1 parallel track, and `clear-the-deck` is visibly out of phase with its current roadmap. `agent-embedding` has healthy momentum, but that makes the imbalance sharper rather than cancelling it out.
