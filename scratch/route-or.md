path: tune

Adjustments are needed this cycle. The chord is not in tune while live tend runtime is unreadable across all four member waves, `signals` is still stuck at design-doc stage despite being a Phase 1 track, and `clear-the-deck` is out of phase with its current roadmap. `agent-embedding` has the strongest momentum, but its open PR is still blocked and widening into shared backend territory rather than cancelling the imbalance.
