path: demo

The embedded terminal build driver ships observable UX changes: palette-launched flows now run inside the app rather than opening Terminal.app, panes reattach after Concerto restart with output intact, and the pane header shows the dispatched agent. The verify script's `--ui` mode already walks the Concerto experience end-to-end. A demo surfaces those changes better than a structural code walk.
