# Larger Task population — 2026-09-25

Current KRs now appear between Product's unchanged objective and its Task plan.
The human explicitly approved keeping the objective as displayed. Three KRs are
illustrative sample content, with no claimed measurements or live planning edit.
Browser assertions verify the exact objective text, three KRs, presentation order
and retention of all 50 Task rows. `wave-krs.png` was captured via `lf screenshot`
and visually inspected. Chapter ownership remains the production contract.

Latest clarification supersedes the original population below: 20 started Tasks
across the sidebar and 50 total Tasks in one Wave. Product now has 50 Tasks and
opens by default in the middle pane. Browser checks prove both exact counts,
full-plan overflow, access to the last Task, and no sidebar admission from
inspection. The fresh `wave-50.png` was captured through `lf screenshot` and
visually inspected; `wave-50-checks.json` records the focused pass.

## Earlier population

The human requested 20–50 Tasks. The large-population URL supplies 43 Tasks in
Loopflow across five Waves, with 27 started and 16 unstarted. Cube retains its
separate one-Task sample. The original small population is unchanged without
the URL parameter. Population content runs before first render and uses the
same outline, Wave pages, selection, workspace and search code.

Focused browser checks pass: exactly 27 sidebar Task rows, actual sidebar
overflow, scoped search, opening a started Task without a Session, the full
Infrastructure plan including backlog, and inspection without sidebar admission.
No JavaScript errors. The 1400×850 capture was made through `lf screenshot`
and visually inspected. Counts and outcomes are in `checks.json`.

This is a sample-data design demo. No native app or live planning changed,
and no performance or native retention result is claimed.
