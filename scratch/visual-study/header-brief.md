# Three Task header treatments
Bounded Opus mockup contribution. Edit ONLY scratch/visual-study/mockups.html. Parent owns comparison wrapper and evidence. No git, PM, installation, server or native changes.

Add optional URL query `header=a|b|c` for current-data Task views; absent param preserve current layout except remove the visible `Linear snapshot · read only` label everywhere in current-data mode (human requested removal). Keep snapshot timestamp in sidebar. Preserve all exact source data, existing navigation, repository switching, Wave page typography, Description label, Task sans-serif font, and linked issue ID.

A: Parent in toolbar. Top bar carries clickable Wave name only, plus existing sidebar toggle. Full Task title and linked ID to its right appear once in page header. Current compact 24px sans heading, comfortable long title wrapping.
B: Title in toolbar. Wave back button then Task title + linked ID in persistent top header; allow header to grow/wrap for long names. No duplicate body heading. Body begins Description. Compact workspace rather than document aesthetic. Preserve usable sidebar toggle and repo picker when hidden.
C: Quiet breadcrumb + page title. Notion-inspired deliberate repetition: muted compact Wave / current Task breadcrumb (ellipsis when narrow, full accessible title), prominent 28px sans page title + linked ID in body with more breathing room between breadcrumb and heading. Make hierarchical difference evident, not two equivalent headings. Clickable Wave.

All options use existing burgundy/cream/Lato palette and existing action dispatch. No new state owner. Select via query parameter only; parent supplies comparison UI. Inspect all variants at long real LOO-285 Task, 1100 and 1400px. Do not take screenshots directly; parent captures with lf screenshot. Finish with concise change summary. Do not edit other files or run broader tests.
