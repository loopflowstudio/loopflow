# Task 3: open Sessions prototype

Build ONLY scratch/visual-study/task-sessions.js and task-sessions.css. No other files, commits or external actions. Read mockups.html and flow-options.css for visual language. User requested Claude for mockups. Parent handles scenario fixtures, integration and wrapper.

Expose window.renderOpenSessions(task, depth), returning HTML. depth 0 = Task Overview: calm compact list of open Sessions (title, provider, status, last message excerpt) above Description; click opens exact Session. depth 1 = Session with context: small Task title + expandable Description context above large conversation surface; depth 2 = Session alone. Use existing global TASKS, SESSIONS, S.ws[task.id].session, S.drafts, esc, issueLink. Parent owns header/zoom. Reuse sessionPane(sid,task.id,true) for dark native-like conversation mock to preserve existing draft handling. Do not create a web chat renderer. Session chrome can be restyled scoped .ts-workspace. Multiple Sessions: compact switcher/tabs above surface, exact title and provider; keep other drafts. One Session: no redundant switcher. Task Overview list uses buttons data-act="selsession" data-id="...", existing parent handler. Provide separate renderTaskSessionContext(task) if helpful but parent expects renderOpenSessions(task,depth). depth1 includes title once with optional <details> Description using task.directiveHtml; no huge title, no runtime diagnostics. depth0 includes heading Sessions + count; no New session button (user undecided about placement, existing Flow header retains it). depth2 conversation fills height.

Sample Session records supplied by parent include title,agent,where:'here',task,id,run,lines:[[who,text]], status:'Your turn'|'Working', excerpt. Prefer yellow status for awaiting human, blue working. No You node headers in Flow. Session state text is allowed. Avoid HERE/ELSEWHERE, empty-state monitoring, fake controls, fabricated paths, sender avatars. Original warm ivory/burgundy/Lato + dark terminal language; reuse colors. Working prototype only. Return through CLI summary.
