# Refine the selected A composition

Human accepted: “A seems like the right default view, and you can zoom > Session with Context > Session”.

Own ONLY scratch/visual-study/mockups.html. Parent owns design, comparison shell, screenshots and interaction verification. User requested Opus for mockups; this is the bounded continuation. No production files, commits, installs, PM changes, subagents or external mutations.

Refine A into ONE continuous workspace with three reversible zoom levels. Keep B/C unchanged as earlier alternatives. Read existing file rather than regenerate it. Preserve the parent's responsive sidebar and inspector fixes.

Accepted: A default, then Session with Context, then Session. Proposed precise interpretation for trying it: overview is existing A with outline, full Task brief and retained Session/companion/Monitor; middle foregrounds the existing selected Session with a compact Task context, hiding the outline and companion/Monitor; deepest foregrounds that same Session with just a minimal identity/back/zoom control. Returning restores the prior pane visibility, Work, draft and selection. This is presentation state only, no new conversation or ownership change. Choose a polished compact zoom control labelled Overview / Session + context / Session (or staged Zoom in/out with equally clear destinations), visible at every level. Focus on tasteful integration with A, not an extra app-level toolbar. Make change of depth spatially comprehensible.

Do not change the stored companion/Monitor visibility to zoom: temporarily present just selected Session, restore underlying workspace on return. No-Session/upcoming Tasks must remain honest and cannot zoom into a fabricated Session. Elsewhere Session cannot be implicitly moved by zoom. Unbound Sessions can zoom with explicit missing Work context. Draft exact contents survive all levels, and zooming out must restore the context panes. Escape steps out of zoom after dismissing any open transient details. Use semantic labelled buttons, visible focus, aria-pressed/current as appropriate, reduced-motion. Task selection resets zoom to overview to avoid another Task inheriting deep zoom.

Support query ?v=a&zoom=0|1|2 for screenshots with safe bounds; default0. Existing direction postMessage remains. Report names/selectors for controls and any exact assumptions. Do not open browser. Parent will do lf screenshots and browser interaction checks. Keep the same brand and original A composition at overview; no redesign of B or C.
