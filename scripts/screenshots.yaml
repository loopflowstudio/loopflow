# Screenshot manifest for Loopflow documentation and UX review
# Run: python scripts/generate_screenshots.py
# Output: docs/screenshots/
#
# Filter by persona: python scripts/generate_screenshots.py --direction conductor

repo: https://github.com/loopflowstudio/loopflow-demos
local: ~/src/loopflow-demos

screenshots:
  # Main dashboard - shows sidebar with grouped waves
  - name: loopflow-main
    type: snapshot
    window_size: [1200, 800]
    mock_loops: true
    directions: [conductor, listener]

  # Running wave selected - detail view
  - name: loopflow-wave-running
    type: snapshot
    window_size: [1200, 800]
    mock_loops: true
    select_branch: wave-auth-feature
    directions: [conductor, improviser]

  # Waiting wave selected - needs interaction
  - name: loopflow-wave-waiting
    type: snapshot
    window_size: [1200, 800]
    mock_loops: true
    select_branch: wave-api-refactor
    directions: [conductor, listener]

  # Empty state - no waves, Quick Experiment visible
  - name: loopflow-empty
    type: snapshot
    window_size: [1200, 800]
    mock_loops: true
    mock_config: empty
    directions: [improviser]

  # Failed wave selected - error state
  - name: loopflow-wave-failed
    type: snapshot
    window_size: [1200, 800]
    mock_loops: true
    select_branch: wave-deploy-fix
    directions: [conductor, listener]

  # Runs tab - wave history with PRs
  - name: loopflow-wave-runs
    type: snapshot
    window_size: [1200, 800]
    mock_loops: true
    select_branch: wave-auth-feature
    select_tab: runs
    directions: [conductor, listener]

  # UI test captures (flow-focused, slower)
  - name: loopflow-main-uitest
    type: uitest
    ui_test_mode: mock-waves
    ui_test_delay: 2

  - name: loopflow-wave-running-uitest
    type: uitest
    ui_test_mode: mock-waves
    ui_test_select_branch: wave-auth-feature
    ui_test_delay: 2

  - name: loopflow-wave-waiting-uitest
    type: uitest
    ui_test_mode: mock-waves
    ui_test_select_branch: wave-api-refactor
    ui_test_delay: 2
