# One binary

Collapse lfd/lfq into lf: the server is `lf wave`, privileged exec routes
through the door inside `lf`, no separate daemon or lfq binaries.

## KRs

- lfd shrinks to a guarded subscription server or disappears into `lf`
  (Linear a806-era item; coordinate with systems' collapse item — one owner,
  this wave).
- No second execution path survives: git, tmux, vendor, and worktree paths
  all route through `lf`.
