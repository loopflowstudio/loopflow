# One fold

One canonical wave fold/query path feeds local `lf`, served `lfd`, and future
remote reach.

## KRs

- No second registry interpretation survives; Concerto, `lf ls/status`, and
  the wave server read the same fold.
- Remaining M1/M2 charter gaps (`stream_events`, `find_repo_root`, worktree
  naming, container-mode residue) reach 0 open code paths.
