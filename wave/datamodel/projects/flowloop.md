# Flowloop

The runtime chain from PR #845: everything agentic is a looping flow with an
agent-set termination bit. This bet finishes the chain.

## KRs

- Steward ships: one warm executive mind per wave owns the human thread; `lf
  chat` becomes the agent bus, fully auditable in its own surface (Linear
  606d59b7).
- Task flowloop v1b: `lf task` drives a red PR green unattended — CI
  classification, reproduce-then-fail, rebase/review folding (33a98266).
- Task flowloop v1c: non-convergence fingerprint, restart ladder, poison
  dead-letter (ac851c0b).
- Project tier wired: waves spawn project loops; subtree caps hold
  (bf2d8709).
- Bare `lf <name>` fallback decided: retired or kept, one documented answer
  (a9dc614d).
