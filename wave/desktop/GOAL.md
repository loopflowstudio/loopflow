---
primary_flow: build
pm:
  provider: asana
  asana_project: '1214270017631632'
---

Concerto is the terminal-first surface that frames waves — portfolio → repo →
wave — hosting the vendor's TUI sessions in embedded panes, never rendering chat
itself.

**Metrics to improve**
- Daily wave work done in-app vs. Terminal.app — toward "the daily driver."
- Wave navigation reads cleanly off the Wave/RepoWork model, not per-repo hacks.
- "Frame, don't render" held — zero native chat surfaces.

**Milestones**
- A+B: the Wave/RepoWork split and the three screens that prove the architecture.
- lfd-owned wave identity: GOAL/MEMORY master, export on PR, pull-in on subscribe.
- Wave-screen UX explored (2–3 variants) and a standing UX iteration loop.
