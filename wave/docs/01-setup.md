# 01: Cross-Platform Init

**Finish line:** `lf init` runs on a fresh Ubuntu machine with OpenCode installed and produces a working config.

Cross-platform init, setup rationalization, platform-aware guidance. Init detects what's present — never installs. Setup entry points (lf init, lfd install, Concerto) have clear ownership and hand off cleanly.

Design doc: `scratch/01-setup.md`
