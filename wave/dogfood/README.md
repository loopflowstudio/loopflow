# Dogfood

Validate loopflow works in real deployment. Mac Mini server, iPhone app, team workflows.

The redesign builds features. Dogfood proves they work end-to-end in the environments real users (starting with us) actually use. Catches the gap between "tests pass" and "this actually works."
