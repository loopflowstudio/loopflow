# goals wave memory

Steers Loopflow toward persistent Goal-driven Waves: goals as the authored loop prompt, Asana as the live roadmap, and Concerto as the session surface.

## Shipped (waveagent model foundation)

- **Two-file wave surface** — `wave/<name>/` is now `GOAL.md` (intent) + `MEMORY.md` (this file). Both are injected into the wave loop's assembled context, so the agent reads its intent and memory each iteration.
- **lfq session cockpit** — designed as `lfq sessions` / `lfq attach <id>` over tmux. ⚠️ 2026-07-02: these subcommands are NOT in the shipped `lfq` CLI (only list/show/create/run/stop/delete/land/logs/usage/providers/auth/repos/token), and the operating prompt's `lfq worker run` doesn't exist either. Dispatch-through-lfd is real (HTTP `POST /v0/waves/{id}/dispatch`) but needs lfd running. Contract and CLI have drifted — see scratch/questions.md.
- **dispatch-through-lfd** — `POST /v0/waves/{id}/dispatch {flow, task}` launches a flow-against-task as its own attachable tmux session (`WaveRunSnapshot.task`); the loop dispatches work as separate sessions instead of running it inline.
- **Demo** — `scripts/demo_waveagent.sh` renders the goals wave prompt and shows MEMORY.md reaching context.

## Model (design settled)

A Wave is a durable named hub — intent (GOAL.md) + memory (MEMORY.md) + work-index (branches/PRs under the name) + a canonical live agent (a `TerminalSession` incarnation). "WaveAgent"/"Dispatch" are roles read off `(source, wave_run_id)`, not new types. Full design: `scratch/waveagent-sessions.md`.

## Roadmap status (2026-07-03 read)

- **Item 1 goal primitive — DONE, on main.** `.lf/goals/<name>.md` resolves + overrides builtins (`engine/flow.rs:740`), builtin goals compile in (`builtins.rs:202`), wave carries `goal:`. Metric "GOAL.md is the authored wave surface" ✓.
- **Item 2 Asana live roadmap — foundation LANDED on main 2026-07-03** (`c113ef04 asana-only: drop Linear/Notion, remove PM mirror and ingestion`; `d370450a lfd: collapse ontology to wave/run/session`). Asana client (`lfd/pm/asana.rs`) + mirror removal on main. Absorbed branch `waveagent-roadmap-remote` + its worktree DELETED 2026-07-03 (diff vs main was empty). REMAINING: wire the live loop to read Asana each iteration + write status back end-to-end (the item's "Done when") — **the next big move once spend_cap lands.**
- **Item 3 wave-model-simplify — in-flight** as open PR #763 (waves: nest per-repo RepoWork).
- **Item 2 wave-budget (`spend_cap`) — DONE on branch `jack-heart.spend-cap`, PR-ready (commit-only, not pushed), 2026-07-03.** SAME uncommitted-worker failure mode recurred: the re-dispatched agent did the test-build fixes + chord rollup + at-limit queue wiring in `loop_ticker.rs` (+151) but left it all UNCOMMITTED. Checkpoint-committed at `70894907`, then finished + committed `140f9802`. Final state: `money.rs` (integer-cents, 4 tests), `spend.rs` (chord rollup + at-limit, 7 tests incl. `two_level_chord_enforces_parent_ceiling_against_sum_of_children`), `loop_ticker.rs` at-limit queue wiring (`small_cap_blocks_once_accrued_cost_crosses_ceiling`, `single_expensive_iteration_trips_per_iteration_ceiling`), `Wave.spend_cap`, `QueueBlockReason::SpendCapExceeded`, migration 042, WaveDto contract test + **Python wire mirror in `models.py` (Money+SpendCap, verified parses capped/uncapped)**. **`cargo test -p loopflow --lib` = 938 pass**; only failure is pre-existing unrelated `journal::terminal_run_events_clear_context` (also fails on main). Swift `Wave` is a curated UI subset (manual dict parse, already omits workers/stack_count) → per CLAUDE.md UI-state carve-out, `spend_cap` lands in Swift when Concerto surfaces it (item 3), NOT speculatively. No `wave.json` DTO fixture exists; WaveDto covered by Rust `contract_tests`. Ready for a human to `lf op land`. **Lesson: dispatched workers keep not committing — always check worktree `git status` for uncommitted work before re-dispatching.**
- **Noise:** PR #767 "goals: draft" is just this loop's own MEMORY/scratch bookkeeping on the goals branch — not real work; ignore/close.

- **Item 2 Asana live roadmap READ-SIDE — DONE + verified on branch `jack-heart.asana-live` (commit `057207b8`, commit-only), 2026-07-03.** Worker committed cleanly this time (broke the uncommitted-worker streak). Added `ops::pm::render_wave_roadmap(repo, wave)` — resolves the wave's linked Asana project (`pm.asana_project` in GOAL.md) via token from the lfd credential store (`get_provider_token("asana")`, no new env/config seam), fetches open tasks via `AsanaClient::list_items`, renders a markdown block into `GoalRenderContext.roadmap`; logs + falls back to the old `wave/<name>` placeholder on any failure so the loop never crashes. Only the looping goal agent fetches; task/worker dispatches pass empty + skip. Grain call: **1 Wave ↔ 1 Asana project, 1 item ↔ 1 task**. 8 roadmap tests pass (fallback, mocked-HTTP render, goal-prompt integration); clippy/fmt clean. Design note: `scratch/asana-live-roadmap.md`.

- **Item 2 Asana live roadmap WRITE-BACK — DONE + verified on branch `jack-heart.asana-live` (commit `a3671934`, commit-only, on top of read-side `057207b8`), 2026-07-03.** Worker committed cleanly (2nd clean worker in a row — streak broken). This CLOSES Item 2's "Done when": read + write-back end-to-end, no local mirror. Design landed: `ops::pm::resolve_asana_task_gid(repo, wave, title)` looks up the gid at **dispatch time** (server-side, from the existing task title — no wire-DTO change) and `launch_worker_run` (`http/routes/waves.rs`) stashes it on the new `Run.asana_task_gid`; `finish_completed_run` (`executor/wave/mod.rs:785`) fires `write_back_task_completion(repo, gid, pr_url)` after `run.pr` is set, guarded on `(gid, pr)` both `Some`, log-and-continue on any Asana failure. Persisted via migration `042_wave_run_asana_task_gid.sql` (⚠️ COLLISION: spend-cap branch also numbers its migration 042 — renumber one when both land). Repair runs inherit the gid. Pure helpers `find_open_task_gid` + `apply_task_completion` unit-tested against mocked Asana HTTP (3 new tests: title-match/skip-done, completes+comments PR link, no-op without gid). `cargo test -p loopflow --lib` = 930 pass (only the pre-existing unrelated `journal::terminal_run_events_clear_context` fails, also on main); clippy/fmt clean. **Item 2 now fully PR-ready as one branch (read+write) for a human to `lf op land`.**

- **Item 2 looping-agent-cloud (backend a) A2 FIRST-CUT — DONE + verified on branch `jack-heart.goal-cloud.20260703` (commit `68e781b9`, off main, commit-only), 2026-07-03.** 3rd clean worker in a row. Adds a `--cloud` flag to `lf goal` (`lf/mod.rs` Goal variant → `bin/lf.rs` → `goal::run(name, once, cloud, cli)`): when set, skips `prepare_goal_launch`/`launch_session` and instead `hand_off_cloud_launch` writes `scratch/goal-launch-<wave>.md` — a scaffold-and-hand-off package the human pastes into codex/claude. `render_cloud_launch_package(wave, message)` reuses the existing `build_goal_message` verbatim (no re-render) and wraps it in 4 sections: (1) session prompt, (2) Asana MCP JSON template w/ `<your-asana-token>` placeholder, (3) recurring-trigger loop instruction (the seam that makes it loop), (4) honest deep-link-back stub (A1's job). +97 lines, 3 files; 1 focused renderer unit test; `cargo test -p loopflow --lib` = 924 pass (only pre-existing unrelated `journal::terminal_run_events_clear_context` fails, also on main); clippy/fmt clean. Chose **A2 over A1** (design lean: respects vendor workflow, no coupling to moving vendor APIs). Standalone off main — independently landable, no stacking on asana-live/spend-cap.
- **Landing backlog (5 PR-ready branches, all commit-only, none pushed — human `lf op land` needed):** `jack-heart.asana-live` (Item 2 read+write), `jack-heart.spend-cap` (Item 2 budget), `jack-heart.goal-cloud` (Item 2 backend-a A2), `jack-heart.vocab-scaffold.20260703` (Item 3 scaffold atom), `jack-heart.vocab-run-integrate.20260703` (Item 3 run+integrate atoms). ⚠️ asana-live + spend-cap BOTH add migration `042_*` on top of main's `041` — real collision, second-lander renumbers to 043 during rebase (can't pre-solve without land order). goal-cloud + both vocab branches add NO migration → collision-free. ⚠️ the two vocab branches are branched off `origin/main` and both touch `builtins.rs` (registration tests) — trivial merge, but land order may need a one-line rebase.

## Item 3 frontier assessment (2026-07-03)

Read of the three Item-3 medium items to pick the next independent move:
- **wave-model-simplify** — the "no member in wave-structure code" half is **effectively already satisfied**: the only `\bmember` hits in rust are flow-group expansion (`engine/flow.rs`, `builtins.rs` — a *direction group's* member directions) and Cargo workspace `members = ["crates/*"]` — different domains, not chord-member vocab. The data-model half (chord = wave with children, two-repo chord) overlaps heavily with **open PR #763** (`waves: split RepoWork out of Wave, rebuild Concerto terminal-first`, branch `jack-heart.waves-outward`) — Jack's active hand-work; DON'T touch concurrently (see [[waves-outward-branch]]).
- **concerto-looping-sessions** — Swift/Concerto UI; collides with the same in-flight #763 Concerto rebuild. Defer.
- **vocabulary-completeness** — greenfield `scaffold`/`run`/`integrate` step atoms. **Independent of #763**, directly advances the "3 reference builds, zero step authoring" metric. → chosen frontier.

Builtin steps are just markdown auto-registered by `build.rs` (drop a file in `builtins/build/step/<name>.md`; `get_builtin_step(name)` resolves it). `scaffold`/`run`/`integrate` all confirmed **absent**.

- **Item 3 vocabulary-completeness `scaffold` atom — DONE + verified on branch `jack-heart.vocab-scaffold.20260703` (commit `3f6e9346`, off main, commit-only), 2026-07-03.** 4th clean worker in a row (uncommitted-worker streak stays broken; independently verified: clean tree, +209 across 3 files). First of the three missing greenfield atoms. `builtins/build/step/scaffold.md` (120 lines, kickoff/implement structure: Orientation/Workflow/Output format/Wave alignment/Principles) is the greenfield-origination step — stands up a project from nothing to a compiles-or-boots state so the follow-on `run` step has something real to run. **Design calls:** (1) infers artifact type (mobile/server/CLI) from the ingested item's *prose*, not a flag (steps are prompts not CLIs); (2) leans on ecosystem generators (`cargo new`, `npm create`, SwiftPM) over hand-rolled boilerplate; (3) writes the chosen platform + build/run command into a `scratch/` scaffold record — the composition seam so `run`/`integrate` inherit the choice instead of re-guessing; (4) ships **standalone, not as a greenfield flow** (a scaffold→implement→run flow needs `run`/`integrate` to exist first to be worth chaining). Auto-registered via `build.rs` (+8 lines in `builtins.rs` = the registration test `scaffold_step_is_registered`, asserts registration + non-stub content). `cargo test -p loopflow --lib` = 926 pass (only the pre-existing unrelated `journal::terminal_run_events_clear_context` fails, also on main); clippy/fmt clean. Standalone off main → collision-free, independently landable.

- **Item 3 vocabulary-completeness `run` + `integrate` atoms — DONE + verified on branch `jack-heart.vocab-run-integrate.20260703` (commit `04724bb8`, off `origin/main`, commit-only), 2026-07-03.** 5th clean worker in a row. Completes the greenfield atom trio **scaffold → run → integrate** (item 3's "missing atoms" #1–3 all built). `run.md` (108 lines) boots the scaffolded artifact and observes real evidence — CLI stdout+exit, server endpoint responses, simulator screen — reading scaffold's scratch-record "Build & run" section to inherit the command (falls back to detecting from `Cargo.toml`/`package.json`, notes inference in questions.md); explicitly contrasts `demo` (narrates, doesn't run) and the `verify` skill (not a step). `integrate.md` (107 lines) drives a client against its server and asserts the round-trip, composing on top of `run` (server runnable first), handling cross-repo client/server and naming any mock/fixture stand-ins. Both prose prompts (platform inferred, no flags), matching scaffold's frontmatter + section layout + autonomy voice; auto-registered by `build.rs`. +232 across 3 files (2 atoms + `run_step_is_registered`/`integrate_step_is_registered` tests). `cargo test -p loopflow --lib` = 927 pass (only pre-existing unrelated `journal::terminal_run_events_clear_context` fails, also on main); clippy/fmt clean. Standalone → collision-free, independently landable. **Remaining item-3 vocab gap: only atom #4 (platform build/test — mobile-sim build + promote `rams` skill→step).** ⚠️ **Worker did `git checkout` in the SHARED working tree (branched off `origin/main`), moving this loop's checkout off the goals branch — had to `git checkout jack-heart.goals.20260703_0901` to restore. Future dispatches must use a separate worktree, not `git checkout` in-place.**

- **⚠️ MEMORY DIVERGENCE (found 2026-07-03):** `origin/main`'s `wave/goals/MEMORY.md` (25 lines) is a *different, more advanced* memory than this goals-branch scratchpad — it records the **Wave/Run/Session ontology collapse** as shipped, `lfq wave run`/`lfq worker run`/`lfq sessions`/`lfq attach` as the **real** runtime surface (contradicting this branch's "those subcommands don't exist" note + scratch/questions.md), the goal primitive as shipped, an **open regression (wave ancestry dropped)**, and different roadmap items (`2-wave-ancestry`, `3-wave-repo-split`). This goals loop has been maintaining a parallel narrative on `jack-heart.goals.*` that never reconciled with what landed on main. **A future iteration (or human) should reconcile: adopt main's memory as the base, re-verify the landing backlog against main's actual state, and refresh the dispatch surface.** Do not trust this branch's "lfq worker run doesn't exist" claim without re-checking against main.

## Next (not yet built)

- **Vocabulary atom #4 — platform build/test.** Mobile simulator build; UI/visual review (promote `rams` from skill to step). Last of item 3's four "missing atoms" (scaffold/run/integrate done). ⚠️ mobile-sim/UI touches Concerto/Swift → check #763 collision before dispatching; the `rams`-skill→step half may be independent.
- **Reconcile goals-branch memory with main** (see divergence note above) — arguably the highest-value next move: the loop is steering off a stale map.
- **Item 2 backend-a A1** (drive vendor cloud API directly + populate the deep-link-back stub) — lower value now that A2 first-cut is PR-ready; design lean was A2.
- Close-the-loop: feed in-flight dispatches + PR state into re-measure (in progress).
- Attention as the loop's human-escalation channel for parked interactive steps.
- `prepare_goal_launch` + the canonical always-on agent session; supervisor + heartbeat.
- Fan-out branch/PR isolation before lifting `workers > 1`.
