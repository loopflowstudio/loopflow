# Attention & navigation

Open the app, land in the right wave. the Mac app owns which wave to open.

## KRs

- The wave list is ranked by attention (failed → waiting → running → idle), and
  each wave carries its reason.
- Wave state, attention, branch/PR context, and terminal status are visible
  around every live session.
