# Compiled checkpoint

`MEMORY.md` is the bounded, curated artifact that survives every boundary.

## KRs

- Answers "what's decided, what's constrained, what's in flight" at a glance
  — typed blocks — and stays prompt-sized as facts accumulate.
- No learning is lost across a land, branch, machine move, or compaction.
- Compiled, deduplicated, and true; raw adds never bloat the file.
