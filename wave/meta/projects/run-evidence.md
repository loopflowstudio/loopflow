# Run evidence

The local ledger explains every run; empirical stats over your own history are
a first-class surface.

## KRs

- Every run is reconstructable locally: prompt, context, flow/skill shape,
  spawned work, cost, time, result (Linear 85307c7).
- Empirical stats over run history ship as a usable view (d11148).
- The quality proxy question has an answer we measure against (99d2d2b).
- Vestigial `waves.direction` / `runs.snapshot_direction` columns dropped.
