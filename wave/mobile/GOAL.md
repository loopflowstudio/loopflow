---
primary_flow: ship-roadmap
mode: manual
workers: 0
metrics:
  - Dormant — no active surface. Reactivate only on a deliberate decision to ship a reader.
---

The Mobile wave is archived (2026-06-19). Loopflow does not ship a mobile
surface — the phone story is the vendors' own apps (Claude, Codex).

Do not invent work here. This wave holds no active loop. If mobile returns, it
returns as a thin reader on the same lfd HTTP API the desktop uses — not a
session host — and only after the desktop layer-above story is solid. Until that
decision is made explicitly, record any pull toward mobile as a blocker for a
human, and stop.
