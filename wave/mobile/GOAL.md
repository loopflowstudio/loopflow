---
primary_flow: build
pm:
  provider: asana
  asana_project: '1214270115507660'
---

This wave is archived: loopflow ships no mobile surface. The phone story is the vendors' own apps (Claude, Codex), and mobile returns only as a thin reader on the desktop lfd HTTP API — never as a session host, and not before the desktop layer-above story is solid.

**Metrics to improve**
- Mobile stays archived; the phone story stays the vendors' Claude/Codex apps.
- If revived, mobile is a thin reader on the desktop lfd HTTP API.
- No mobile session host and no phone-specific pairing infra survive.

**Milestones**
- Keep the wave dormant until the desktop layer-above story is solid.
- If revived, scope mobile as a read-only client on the existing lfd HTTP API.
