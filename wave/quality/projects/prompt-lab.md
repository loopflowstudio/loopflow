# Prompt lab

Edit what the model sees, justified by evidence — never vibes.

## KRs

- 5 prompt/context changes land with a cited local run failure or cost trace
  and a follow-up run showing the intended behavior.
- "What does a measured prompt pass look like" answered and repeatable
  (Linear question item).
- General-vs-taste boundary tested on both loopflow and Cadenza before a
  lesson moves into builtins (Linear question item; loopflow.md boundary).
